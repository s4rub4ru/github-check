import {sum} from "./sum"

const result = sum(1, 2)
console.log("resultado", result)

//console.log("hello World");

/*const fullname = "John Doe"
const age = 1
const isDeveloper  = true
const hobbies = ["reading", "coding", "swimming"]

const street = {
    name: "Main Street"
}

const address = {
    street
}

const person = {
    name: "John",
    age: 30,
    hobbies: ["reading", "coding", "swimming"],
    email: "a@gmal.com",
    address: {
        city: "New York",
        Street: "Main St."
    }
}

console.log(person["name"])
console.log(person.name)

const sum = 1 - 2

function sum(a: number, b: number) {
    console.log(a + b)
}

sum(1, 2)
sum(4, 2)

function suma(a: number, b: number): number {
    return a + b
}

const edad: number = 30

type Person = {
    name: string
    age: number
}
function isDeveloper(person:Person) {
    if (person.age >= 18) {
        console.log("is a developer")
    }
    else{
        console.log("is not a developer")
    }
}

isDeveloper(person)

//string, number, boolean, object

const fullname: string = "John"
const age: number = 30
const isAdult: boolean = true
const hobbies: string[] = ["reading", ]*/